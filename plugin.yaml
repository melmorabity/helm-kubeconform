---
name: kubeconform
version: 0.6.2-1
usage: Validate Helm charts using Kubeconform
description: This plugin validates Helm charts using Kubeconform
ignoreFlags: false
command: python $HELM_PLUGIN_DIR/plugin.py
platformCommand:
hooks:
  install: $HELM_PLUGIN_DIR/install.sh
  update: $HELM_PLUGIN_DIR/install.sh
